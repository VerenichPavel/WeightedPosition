<Window x:Class="TestApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        Title="Расчет взвешеннго пересечения" Height="720" Width="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <DockPanel Grid.Column="0" Margin="8">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Расчёт"
                        Command="{Binding CalculateCommand}"
                        CommandParameter="{Binding ElementName=MapCtrl}"
                        Width="80" Margin="0,0,8,0"/>
                <Button Content="Очистить"
                        Command="{Binding ClearCommand}"
                        CommandParameter="{Binding ElementName=MapCtrl}"
                        Width="80"/>
            </StackPanel>

            <TextBlock DockPanel.Dock="Top" Text="Станции (Name Lat Lon Bearing Quality):" FontWeight="Bold" Margin="0,0,0,6"/>
            <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     FontFamily="Consolas"
                     FontSize="13"/>
        </DockPanel>

        <!-- Карта -->
        <mapsui:MapControl x:Name="MapCtrl" Grid.Column="1" Margin="8"/>
    </Grid>
</Window>
